{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug (native)",
      "cargo": {
        "args": ["build", "--bin=savior-game"],
        "filter": { "name": "savior-game", "kind": "bin" }
      },
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
